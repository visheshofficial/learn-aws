name: 'athena_federation'
version: '1.0.0'
config-version: 2

profile: 'athena_federation'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  athena_federation:
    staging:
      +materialized: table
    marts:
      +materialized: table
